[название функции].call(объект, 10,2)
[название функции].apply(объект, [10,2])

bind - TO BE STUDIED

Клонирование объектов - через цикл, Object.assign. Тут не забываем, что не работает ,
если есть вложенный обхект! Главный способ - через JSON, сериализацию.
JSON.stringify() - в строку
JSON.parse() - в код

