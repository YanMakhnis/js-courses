1) Используя for, while и do…while - реализуйте один и тот же цикл, который выведет в консоль все чётные числа, от 2 до 12.

2) Скопируйте и вставьте следующий кусок кода. С помощью инструмента разработчика - выясните, какое значение будет иметь переменная в момент, отмеченный комментарием.

	function getX() {
		x = Math.floor(Math.random() * 99 + 23)
		x += 3
		x *= 2
		x -= 4
		x /= 2
		x **= 2
		x -= 10
		// Чему равен X сейчас?
		x %= 5
		x *= 3
		x += 1
		alert(x)
	}
	getX()

3) Напишите цикл, который выведет таблицу умножения на 7.
	Результат должен быть сообщениями в консоли:
	7х1=7
	7х2=14
	И т.д.

4) Напишите функцию min(a,b), которая возвращает меньшее из чисел a и b.
	Пример работы функции:
	min(2, 5) => 2
	min(3, -1) => -1
	min(1, 1) => 1
	1*) Добавить проверку, что оба переданных параметра являются числами
		В противном случае вернуть строку "Извините, вы передали неверные значения"

5) Исправьте код, чтобы он работал корректно:
let age = prompt("Сколько Вам лет?", 18);
// в зависимости от условия объявляем функцию
if (age < 18) {
  let welcome = function () {
    alert("Привет!");
  }
} else {
  let welcome = function () {
    alert("Здравствуйте!");
  }
}
// ...не работает
welcome();

6) Напишите функцию sumTo(n), которая вычисляет сумму всех целых чисел 1 + 2 + ... + n.
	1*) Сделайте два варианта решения:
		1) С использованием цикла.
		2) Через рекурсию.
	Пример работы:
	sumTo(1) => 1
	sumTo(2) => 2 + 1 = 3
	sumTo(3) => 3 + 2 + 1 = 6
	sumTo(4) => 4 + 3 + 2 + 1 = 10

